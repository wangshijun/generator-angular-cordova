language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-cordova' ]; then cd .. && eval "mv $currentfolder generator-angular-cordova" && cd generator-angular-cordova; fi

